server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_scheme
    username: root
    password: root


#mybatis:
#一、使用mybatis - config. xml时的配置项
#  config-location: classpath:mybatis-config.xml

  #二、代替mybatis-config.xml的配置项
#  mapper-locations: classpath*:mybatis/mapper/*.xml
#  type-aliases-package: com.duing.bean